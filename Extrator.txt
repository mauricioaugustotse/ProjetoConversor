javascript:(function(){function extractAndClean(){let contentElement=document.querySelector('#content');if(!contentElement){alert('❌ Container de conteúdo principal (#content) não encontrado.');return null;}let clone=contentElement.cloneNode(true);clone.querySelectorAll('.documentActions').forEach(el=>el.remove());let text=clone.innerText||clone.textContent||'';if(!text){return'';}let cleanText=text.replace(/(\r\n|\n|\r){3,}/gm,'\n\n').replace(/[ \t]+/g,' ').trim();return cleanText;}const finalText=extractAndClean();if(finalText){navigator.clipboard.writeText(finalText).then(()=>{const notification=document.createElement('div');notification.innerHTML='✅ Temas do TSE extraídos e copiados!';Object.assign(notification.style,{position:'fixed',top:'20px',right:'20px',background:'#28a745',color:'white',padding:'16px',borderRadius:'8px',zIndex:'999999',fontFamily:'sans-serif',fontSize:'16px',boxShadow:'0 5px 15px rgba(0,0,0,0.3)'});document.body.appendChild(notification);setTimeout(()=>notification.remove(),3000);}).catch(err=>{prompt('Erro ao copiar. Selecione e copie manualmente (Ctrl+C):',finalText);});}else{alert('❌ Nenhum conteúdo de mérito foi extraído.');}})();